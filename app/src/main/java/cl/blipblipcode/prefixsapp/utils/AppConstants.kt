package cl.blipblipcode.prefixsapp.utils

object AppConstants {
    object Prefs {
        const val NAME = "spam_blocker_prefs"
        const val KEY_SKIP_CALL_LOG = "skip_call_log"
        const val KEY_SKIP_NOTIFICATION = "skip_notification"
        const val KEY_LEGACY_SCREENING_CONFIGURED = "legacy_screening_configured"
    }

    object Assets {
        const val COUNTRY_DIALING_CODES_FILE = "country_dialing_codes.json"
    }
}
